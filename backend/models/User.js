// User model placeholder
// Define user schema based on chosen database
class User {
  constructor(username, email, password) {
    this.username = username;
    this.email = email;
    this.password = password;
  }
}

module.exports = User;
